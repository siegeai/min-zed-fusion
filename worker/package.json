{
  "name": "ask-min-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker behind the Ask min. widget on getmin.ai",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.90.0"
  }
}
